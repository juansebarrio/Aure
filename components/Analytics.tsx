/**
 * Hueco para medición (GA4 / Meta Pixel).
 *
 * TODO(medición): NO instalar nada todavía — faltan claves y la definición del
 * cliente. Cuando se confirme, acá van los <Script> de GA4 / Meta Pixel usando
 * NEXT_PUBLIC_GA4_ID y NEXT_PUBLIC_META_PIXEL_ID, respetando el consentimiento
 * de cookies (ver /politicas#cookies).
 */
export function Analytics() {
  return null;
}
